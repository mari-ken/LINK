$('#calendar').fullCalendar(
  'renderEvent',
  $.parseJSON("<%=j render(@event, format: :json).html_safe %>"),
  true
);
$('.modal').modal('hide');
$("#list-events").html("<%= j(render 'users/event', events: @events) %>");